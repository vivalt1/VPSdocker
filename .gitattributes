# Git LFS 追踪规则
# 运行: git lfs install && git lfs track "*.tar.gz" "*.tar"
# 然后提交 .gitattributes
*.tar.gz filter=lfs diff=lfs merge=lfs -text
*.tar filter=lfs diff=lfs merge=lfs -text
# 备份清单等文本文件正常跟踪
*.txt filter=lfs diff=lfs merge=lfs -text
